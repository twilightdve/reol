# Claude Code 運用方針

## 基本方針

通常作業では Claude Sonnet 5 を使用する。

Claude Sonnet 5 は、日常的な実装、リファクタリング、調査、ドキュメント作成、コードレビューの主力モデルとして扱う。

最初から最大性能のモデルや最大 effort を使わない。
まずは必要十分なコストで進め、失敗や難易度の上昇が確認できた場合だけ、段階的に effort またはモデルを上げる。

## effort 方針

通常は medium effort を使用する。

### low effort を使ってよい作業

以下のような、範囲が明確で失敗リスクが低い作業では low effort でよい。

* フォーマット修正
* 変数名や関数名の変更
* 単純な文言修正
* boilerplate の追加
* import 整理
* コメントの軽微な修正
* 小さな型修正
* 明確な指示に基づく単純な置換

### medium effort を使う作業

以下のような通常作業では medium effort を使う。

* 小〜中規模の実装
* 既存コードの読み解き
* 軽いバグ修正
* 簡単なリファクタリング
* ドキュメント作成
* 設定ファイルの調整
* テスト追加
* APIやコンポーネントの軽微な変更
* 要件が比較的明確なタスク

### high effort を使う作業

以下の場合は high effort を使う。

* 原因がはっきりしないバグ調査
* 複数ファイルにまたがる修正
* 既存設計への影響がある変更
* アーキテクチャ判断
* データ構造や責務分離の見直し
* テストが落ちる原因の追跡
* 仕様の曖昧さを整理しながら実装する作業
* セキュリティ、認可、課金、データ永続化に関わる変更

### xhigh effort を使う作業

以下の場合だけ xhigh effort を使う。

* 同じ問題に一度失敗した後の再調査
* 難しい不具合の根本原因分析
* 大きめのリファクタリング
* 影響範囲が広い移行作業
* 複雑な設計判断
* 正確性がコストや速度より重要な作業
* 一度間違えると後戻りが高くつく作業

### max effort について

max effort は標準では使わない。

本当に重要な一回限りの高難度作業で、追加コストを払ってでも品質を優先する場合だけ使用を検討する。

max effort を使う前に、Opus 4.8 に切り替えた方が適切ではないかを確認する。

## モデルルーティング方針

通常は Claude Sonnet 5 から開始する。

以下の場合のみ Claude Opus 4.8 への切り替えを検討する。

* Sonnet 5 が同じタスクで2回失敗した
* 推論の深さが明らかに足りない
* 複雑な設計判断が必要
* 正しさの証明や厳密な検討が必要
* 影響範囲が広く、失敗時の修正コストが高い
* 高難度のバグで、局所修正ではなく根本原因の理解が必要

「重要そうだから」という理由だけで Opus に切り替えない。
切り替えは、失敗、曖昧さ、リスク、推論不足といった具体的な根拠がある場合に限る。

## 作業の進め方

十分な情報がある場合は、質問で止まらず作業を進める。

ただし、以下の場合は作業前に確認する。

* ファイル削除
* 大きな設計変更
* 外部サービスへの送信
* 本番環境に影響する操作
* 課金や契約に関わる操作
* 認証情報やシークレットに関わる操作
* データベースの破壊的変更
* ユーザーの明示指示と異なる判断が必要な場合

## 実装方針

不要な大規模リファクタリングはしない。

依頼された目的に対して、最小限で安全な変更を優先する。

ただし、既存コードの問題が目的達成を妨げる場合は、必要な範囲で整理してよい。

変更する場合は、以下を意識する。

* 既存の設計思想を尊重する
* 既存の命名規則に合わせる
* 既存のディレクトリ構成に合わせる
* 既存のテスト方針に合わせる
* 変更範囲を広げすぎない
* 将来の仮定だけで抽象化しない

## 検証方針

可能な場合は、変更後に検証を行う。

優先する検証は以下。

* 型チェック
* lint
* unit test
* build
* 該当機能の簡易動作確認
* 差分確認

検証できなかった場合は、検証できなかった理由を明示する。

未検証の内容を、検証済みのように報告しない。

## 進捗報告方針

進捗を報告する場合は、このセッション内で実際に確認した内容だけを述べる。

未確認の推測を、完了済みの作業のように言わない。

「おそらく」「未確認」「推測」「要確認」の区別を明確にする。

## 最終報告

作業完了時は、以下の順で簡潔に報告する。

1. 結論
2. 変更した内容
3. 検証した内容
4. 残っている懸念
5. 次に必要な作業があればその内容

内部思考を逐語的に説明する必要はない。
ユーザーが判断しやすい形で、結果と根拠を整理して報告する。
